#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int numz;

	for (numz = '0'; numz <= '9'; numz++)
		putchar(numz);
	putchar('\n');
	return (0);
}
